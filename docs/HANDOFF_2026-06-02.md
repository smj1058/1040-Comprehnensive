# Handoff — Definitions Library + Tax KB layer · 2026-06-02

This document captures the documentation/standards work executed on the
`claude/md-library-headers-definitions-zSoQP` branch (merged to the default
branch `claude/update-exec-summary-notion-118m7` via PR #1). Copy this into
`C:\Users\SethJohnson\MCP_Projects\ExecSumm-Dossiers\` as the canonical record of
what happened, what's in flight, and what's deferred.

> **Note on where things actually live.** This was a **cloud (Claude Code on the
> web)** session — an isolated Linux container with **no access to `C:\` or
> `G:\`**. Everything below was written to **Git** (repo) and **Notion** only.
> Nothing was written to your local `MCP_Projects` folder automatically; this
> file has to be copied there by you or a local (Desktop) Claude with filesystem
> access.

---

## Run summary

| Metric | Value |
| --- | --- |
| Session type | Documentation / standards (no client dossiers touched) |
| Branch | `claude/md-library-headers-definitions-zSoQP` → merged into default `claude/update-exec-summary-notion-118m7` |
| PR | #1 — **merged** (merge commit `471bc69`) |
| New docs | 3 (`DEFINITIONS_LIBRARY.md`, `TAX_KB_LIBRARY.md`, `PLAN_TRACKER_…md`) + this handoff |
| CLAUDE.md change | Added hard rule **#12** (client key mandatory in every dossier §0) |
| Notion change | Added **`📄 KB Doc`** URL property to the Tax Technical Knowledgebase |
| Client data changed | None (docs-only) |

---

## What was done (DONE)

1. **`docs/DEFINITIONS_LIBRARY.md`** (415 lines · sha256 `b09d1e45f118…`) — the single
   lookup reference for every header, DB property, status symbol, and source tag
   across the pipeline (dossiers, Exec Summaries, Client Email Log, Meetings
   Tracker, workbooks, §8/§9 tables). Establishes the **three-axis findability
   model**: (A) headers + source tags inside the file = primary search key;
   (B) Notion DB row + properties = pointer-of-record; (C) filename + folder =
   convenience/dated trail. **Bottom line: filename is not the search key —
   headers + DB properties are.** Linked from README.
2. **`CLAUDE.md` rule #12** — `Client #` + `Mango Client ID` are now MANDATORY in
   every dossier §0, resolved from the Accounts record, never fabricated; explicit
   gap flag + §7 action item required if the Account has no number.
3. **`docs/TAX_KB_LIBRARY.md`** (163 lines · sha256 `46f0b88b53e3…`) — spec for the
   markdown-file + pointer layer over the **live Tax Technical Knowledgebase**.
   Keyed by **`Entry ID`** (auto-increment). Mirrors the dossier pointer model
   (structured props + Summary in Notion; full body in the `.md`; row's `📄 KB Doc`
   points at it). **NOTE:** this was rewritten mid-session (commit `1efcc1b`) after
   discovering the real DB — any copy saved before that is the wrong archived-V2
   version.
4. **`📄 KB Doc` URL property** added to the Tax Technical Knowledgebase in Notion.
5. **`docs/PLAN_TRACKER_LibraryAndTaxKB_2026-06-02.md`** (88 lines · sha256
   `4964a1282ab8…`) — live TODO/decision tracker.
6. **PR #1 merged** to the default branch; session unsubscribed from the PR.

---

## Key findings

- **Client-# audit (all 42 dossiers):** ~30 carry a real client number in §0; ~11
  say "not populated on Account record" (the Notion **Account** itself lacks a
  number — an upstream data gap, not a file problem). The earlier "1 missing"
  (`ReneeMuellerJeffCohn`) was a **false positive** — it carries
  `Client #: 18051006-01` / Mango `937741` in an **inline `**Client #:**` header**
  rather than a `| Client # |` table row. **No dossier is actually missing its key.**
  (Two valid §0 header styles exist: table-row and inline-bold — both carry the key.)
- **Tax KB DB already exists and is seeded** — do NOT build a new one:
  - DB: **Tax Technical Knowledgebase** — `857b8e97-7b2f-4b5c-9b0f-6f23300936d4`
  - data source: `collection://c1c33884-96fa-42a0-bf6c-6ac7776e26dd`
  - durable key: **`Entry ID`** (auto-increment) + `Topic` (title)
  - rich schema: Domain, Tax Category, Taxpayer Type, Jurisdiction, Return/Form,
    Authority Level, Code Section, Complexity/Risk, Timing/Phase, Status, Conflict
    Flag + Reconciliation Note, Last Reviewed, Key Thresholds, Notes/Gotchas, …
  - existing flag views: **⚠️ Conflict Queue**, **Planning Playbook**, **By Domain**
  - seeded entries: PTET, NIIT, QSBS §1202, SE Tax, Estimated Tax & Safe Harbor,
    C-Corp Double Taxation, FBAR/FATCA, Foreign Tax Credit, Child Tax Credit,
    Standard Deduction, Tax-Loss Harvesting, …
- **IGNORE / archived:** "Spec: Tax Question System V2"
  (`notion.so/33149172875181cf847aed6f3d5c0d0f`, under "_Archive (Mirrored from
  Accruity)") — superseded by the live DB above.

---

## NOT DONE / open (deferred)

- [ ] **Assign Client #** on the ~11 Notion **Accounts** that lack one (firm-side —
      cannot be invented): Aidan McIsaac, Brittany Byma, Daniel Huffman,
      David & Michelle Saward, Gary Aronov, Jeremy Martin, Logan Bowles,
      Michael Venereo, Michelle Bowles, Renee Mueller Solo, Tommy Harr.
- [ ] **Build the KB MD layer:** `.md` template/renderer keyed off `Entry ID`
      (a `tax_kb/` module mirroring `execsumm_emails/`); gap+stale detector modeled
      on `opportunity_flags.py`; backfill `.md` files for the rows already in the DB.
- [ ] **Confirm the SharePoint folder path** for `Tax-Knowledge/<Domain>/` (subledge tenant).
- [ ] **Standalone `client-intelligence` repo** — could not be created from the
      cloud session (GitHub integration scoped to this repo only). Needs the user
      to create it + grant the GitHub App access.

---

## Hard boundaries (carried from CLAUDE.md)

- **Do NOT touch Make.com** scenarios/hooks/data stores/connections (rule #8). KB
  auto-create *triggers* stay in Make; only the file-creation + pointer-stamp step
  is ours, defined as a contract in `docs/TAX_KB_LIBRARY.md` §4.
- **Pointer-only Notion model** — body lives in the `.md`; the DB row points at it.

---

## Reference paths for future Claude sessions

- **Canonical session instructions:** `CLAUDE.md` (repo root)
- **The new library:** `docs/DEFINITIONS_LIBRARY.md`
- **Tax KB spec:** `docs/TAX_KB_LIBRARY.md`
- **Per-client invocation contract:** `docs/BATCH_REFRESH_PROMPT.md`
- **Prior handoff:** `docs/HANDOFF_2026-04-22.md` (42-dossier batch run)
- **Reference dossiers:** `dossiers/SpringBengtzen/dossier.md` · `dossiers/TommyHarr/dossier.md`

---

## Branch / git state

- **Branch:** `claude/md-library-headers-definitions-zSoQP`
- **PR #1:** merged into default `claude/update-exec-summary-notion-118m7` (`471bc69`)
- **Authoritative doc versions** (byte-compare against any local copy):
  - `docs/DEFINITIONS_LIBRARY.md` — 415 lines — `b09d1e45f118…`
  - `docs/TAX_KB_LIBRARY.md` — 163 lines — `46f0b88b53e3…`
  - `docs/PLAN_TRACKER_LibraryAndTaxKB_2026-06-02.md` — 88 lines — `4964a1282ab8…`

---

## Action items for Accruity ops side

- [ ] **Assign the 11 missing Account Client #s** in Notion (then they flow into dossiers on next refresh)
- [ ] **Confirm the SharePoint `Tax-Knowledge/` folder path** for the KB `.md` files
- [ ] **Decide KB build scope** (template / detector / backfill) and greenlight
- [ ] **Create the standalone `client-intelligence` repo** + grant GitHub App access (if wanted)
- [ ] **Copy this handoff + the 3 docs into `C:\…\MCP_Projects\ExecSumm-Dossiers\`** (cloud session can't)

---

*This handoff doc captures the state as of 2026-06-02. Copy into
`C:\Users\SethJohnson\MCP_Projects\ExecSumm-Dossiers\` as the canonical record of
this session.*
